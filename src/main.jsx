import React from "react";
import ReactDOM from "react-dom/client";
import CeloCrush from "./CeloCrush.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CeloCrush />
  </React.StrictMode>
);
